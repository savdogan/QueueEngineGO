{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch file (Dosyayı Başlat)",
            "type": "go",
            "request": "launch",
            "program": "main.go"
        },
        {
            "name": "Launch package (Paketi Başlat)",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}"
        }
    ]
}